{% extends 'base.html.twig' %}

{% block title %}Edit Activiteit{% endblock %}

{% block body %}
    <h1>Edit Activiteit</h1>

    {{ include('activiteit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('activiteit_index') }}">back to list</a>

    {{ include('activiteit/_delete_form.html.twig') }}
{% endblock %}
