{% extends 'base.html.twig' %}

{% block title %}Activiteit{% endblock %}

{% block body %}
    <h1>Activiteit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activiteit.id }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ activiteit.datum ? activiteit.datum|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Tijd</th>
                <td>{{ activiteit.tijd ? activiteit.tijd|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activiteit_index') }}">back to list</a>

    <a href="{{ path('activiteit_edit', {'id': activiteit.id}) }}">edit</a>

    {{ include('activiteit/_delete_form.html.twig') }}
{% endblock %}
