<form method="post" action="{{ path('activiteit_delete', {'id': activiteit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activiteit.id) }}">
    <button class="btn">Delete</button>
</form>
